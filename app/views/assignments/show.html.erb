<div class="container container-table">
    <div class="row verticle-center-row">
        <div id="assignmentForm" class="col-md-4 col-md-offset-4 well">
            <h1 class="text-center">Assignment:</h1>
            <%= render 'form' %>
            <%= button_to('Complete', assignment_path(@assignment),
                          method: "delete", data: { confirm: "Are you sure you completed the assignment?" }) %>
        </div>
    </div>
</div>

