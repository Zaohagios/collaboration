$("ul.errors").html("")
<% if @wash.errors.any? %>
  <% @wash.errors.full_messages.each do |msg| %>
    $("ul.errors").append($("<li />").html("<%= msg.html_safe %>"))
  <% end %>
<% else %>
    location.reload()
    $('#modal-editing').modal("hide")
<% end %>
